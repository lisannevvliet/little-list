<div id="trivia">
    <section> 
        <h1><span id="name">{{name}}</span>'{{#if s}}s{{/if}} trivia</h1>
        <p id="connected"></p>
        <p id="question"></p>
        <div id="answers">
            <button></button>
            <button></button>
            <button></button>
            <button></button>
        </div>
    </section>
    <section id="chat"> 
    <h1><span id="name">{{name}}</span>'{{#if s}}s{{/if}} chat</h1>
    <span id="spacing"></span>
    <ul></ul>
    <p id="typing"></p>
    <form>
        {{!-- https://stackoverflow.com/questions/13766015/is-it-possible-to-configure-a-required-field-to-ignore-white-space --}}
        <input type="text" autocomplete="off" placeholder="Type a message" maxlength="280" pattern=".*\S+.*" required>
        <button type="submit">
            <img src="images/sent.png">
        </button>
    </form>
</section>
</div>